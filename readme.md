# This is a simple script to encrypt / decrypt secrets

## Features
- Interactive CLI menu
- Obfuscate passwords with Vigenère cipher
- Encrypt passwords using Fernet encryption
- Use both encryptions (stacked)
- Encrypt/decrypt single password
- Encrypt/decrypt from `txt` files
- Load Fernet key from file or type your key (string is then converted to Fernet key)